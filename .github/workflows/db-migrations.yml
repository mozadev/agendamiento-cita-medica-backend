name: Database Migrations

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environment to run migrations'
        required: true
        type: choice
        options:
          - dev
          - staging
          - prod
      migration_action:
        description: 'Migration action'
        required: true
        type: choice
        default: 'apply'
        options:
          - apply    # Apply pending migrations
          - verify   # Only verify connection and status
          - force    # Force re-apply all migrations (dangerous)

env:
  AWS_REGION: us-east-1

jobs:
  run-migrations:
    name: Run Database Migrations
    runs-on: ubuntu-latest
    environment: ${{ github.event.inputs.environment }}
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Get Lambda Function Name
        id: get-lambda
        run: |
          ENV="${{ github.event.inputs.environment }}"
          # El nombre de la función en SAM es: ${Environment}-db-migration
          FUNCTION_NAME="${ENV}-db-migration"
          echo "function_name=$FUNCTION_NAME" >> $GITHUB_OUTPUT
          echo "🔍 Buscando Lambda function: $FUNCTION_NAME"
          
          # Verificar que la función existe
          if aws lambda get-function --function-name "$FUNCTION_NAME" --region ${{ env.AWS_REGION }} >/dev/null 2>&1; then
            echo "✅ Lambda function encontrada: $FUNCTION_NAME"
          else
            echo "❌ Lambda function no encontrada: $FUNCTION_NAME"
            echo ""
            echo "🔍 Listando funciones Lambda disponibles..."
            aws lambda list-functions \
              --region ${{ env.AWS_REGION }} \
              --query 'Functions[?contains(FunctionName, `db-migration`) || contains(FunctionName, `migration`)].FunctionName' \
              --output table || echo "No se pudieron listar funciones"
            echo ""
            echo "💡 Asegúrate de que el deploy de SAM se haya completado exitosamente"
            exit 1
          fi

      - name: Verify Migration Action
        run: |
          ACTION="${{ github.event.inputs.migration_action }}"
          ENV="${{ github.event.inputs.environment }}"
          
          echo "🎯 Acción de migración: $ACTION"
          echo "🌍 Ambiente: $ENV"
          
          if [ "$ACTION" == "force" ] && [ "$ENV" == "prod" ]; then
            echo "⚠️  ADVERTENCIA: Force migration en producción requiere confirmación adicional"
            echo "   Esta operación es potencialmente destructiva"
          fi

      - name: Invoke Database Migration Lambda
        id: invoke-migration
        timeout-minutes: 20
        run: |
          FUNCTION_NAME="${{ steps.get-lambda.outputs.function_name }}"
          ACTION="${{ github.event.inputs.migration_action }}"
          
          echo "🚀 Invocando Lambda function: $FUNCTION_NAME"
          echo "📝 Acción: $ACTION"
          echo ""
          
          # Preparar payload para Lambda
          PAYLOAD=$(cat <<EOF
          {
            "action": "$ACTION",
            "country": "both"
          }
          EOF
          )
          
          echo "📤 Invocando función..."
          RESPONSE=$(aws lambda invoke \
            --function-name "$FUNCTION_NAME" \
            --payload "$PAYLOAD" \
            --region ${{ env.AWS_REGION }} \
            --cli-read-timeout 900 \
            /tmp/lambda-response.json 2>&1)
          
          EXIT_CODE=$?
          
          if [ $EXIT_CODE -ne 0 ]; then
            echo "❌ Error invocando Lambda:"
            echo "$RESPONSE"
            exit 1
          fi
          
          # Leer respuesta
          if [ -f /tmp/lambda-response.json ]; then
            RESPONSE_BODY=$(cat /tmp/lambda-response.json)
            echo "📥 Respuesta de Lambda:"
            echo "$RESPONSE_BODY" | jq '.' || echo "$RESPONSE_BODY"
            
            # Verificar si hubo error
            ERROR=$(echo "$RESPONSE_BODY" | jq -r '.errorMessage // empty' || echo "")
            if [ ! -z "$ERROR" ]; then
              echo "❌ Error en Lambda: $ERROR"
              echo "📋 Stack trace:"
              echo "$RESPONSE_BODY" | jq -r '.stackTrace // empty' || echo "No stack trace"
              exit 1
            fi
            
            # Extraer resultados
            PERU_RESULT=$(echo "$RESPONSE_BODY" | jq -r '.body | fromjson | .results.peru // empty' || echo "")
            CHILE_RESULT=$(echo "$RESPONSE_BODY" | jq -r '.body | fromjson | .results.chile // empty' || echo "")
            
            if [ ! -z "$PERU_RESULT" ]; then
              echo ""
              echo "📊 Resultado Perú:"
              echo "$PERU_RESULT" | jq '.' || echo "$PERU_RESULT"
            fi
            
            if [ ! -z "$CHILE_RESULT" ]; then
              echo ""
              echo "📊 Resultado Chile:"
              echo "$CHILE_RESULT" | jq '.' || echo "$CHILE_RESULT"
            fi
          else
            echo "⚠️  No se pudo leer la respuesta de Lambda"
          fi
          
          echo ""
          echo "✅ Migración completada"

      - name: Migration Summary
        if: always()
        run: |
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "📊 RESUMEN DE MIGRACIÓN"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
          echo "Ambiente: ${{ github.event.inputs.environment }}"
          echo "Acción:   ${{ github.event.inputs.migration_action }}"
          echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"

